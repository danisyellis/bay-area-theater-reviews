{
  "name": "Vinyl",
  "version": "1.0.0",
  "description": "A community for theater enthusiasts in the Bay Area to review their favorite shows.",
  "engines": {
    "node": "6.x"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:reset": "npm run db:drop && npm run db:create && npm run db:schema && npm run db:seed && npm run db:session:setup",
    "db:create": "createdb theater_dev",
    "db:drop": "dropdb theater_dev",
    "db:schema": "psql -d theater_dev < src/models/db/schema.sql",
    "db:seed": "psql -d theater_dev < src/models/db/shows.sql",
    "db:session:setup": "psql theater_dev < node_modules/connect-pg-simple/table.sql",
    "test": "./node_modules/.bin/eslint src"
  },
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^1.0.3",
    "body-parser": "^1.17.2",
    "connect-pg-simple": "^4.2.1",
    "dotenv": "^4.0.0",
    "ejs": "^2.5.6",
    "express": "^4.15.3",
    "express-session": "^1.15.6",
    "morgan": "^1.9.0",
    "pg": "^6.2.4",
    "pg-promise": "^7.3.1"
  },
  "devDependencies": {
    "eslint": "^4.4.1",
    "eslint-config-airbnb-base": "^11.3.1",
    "eslint-plugin-import": "^2.7.0",
    "nodemon": "^1.11.0"
  }
}
